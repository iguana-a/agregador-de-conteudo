import { c as getKnexTimestampPrefix, i as TSFileMigrationProvider, r as FileSeedProvider, t as Seeder } from "./seeder-DGlLMoe8.mjs";

//#region src/config/define-config.mts
const defineConfig = (input) => input;

//#endregion
//#region src/config/dummy-dialect-config.mts
function DUMMY_DIALECT_CONFIG() {
	throw new Error("`DUMMY_DIALECT_CONFIG` is a dummy dialect config that should be overriden by environment-specific configuration. You must run the command with the `-e <environment>` flag and make sure there is a configuration for `<environment>` in your `kysely.config` file.");
}

//#endregion
export { DUMMY_DIALECT_CONFIG, FileSeedProvider, Seeder, TSFileMigrationProvider, defineConfig, getKnexTimestampPrefix };